# ifttt-file-trigger

```
usage: ifttt-daemon.py [-h] [--wait WAIT] cmd trigger_on trigger_off

positional arguments:
  cmd
  trigger_on
  trigger_off

optional arguments:
  -h, --help   show this help message and exit
  --wait WAIT
  ```
